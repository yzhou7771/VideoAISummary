# OpenAI API Configuration
OPENAI_API_KEY=sk-xxxx

# AI Models Configuration  
# Whisper 模型（默认 whisper-1）
WHISPER_MODEL=whisper-1
# 摘要模型（建议 gpt-4o-mini 成本低）
SUMMARY_MODEL=gpt-4o-mini

# System Configuration
# 临时文件目录
TMP_DIR=./tmp

# YouTube Download Configuration (Optional)
# To bypass YouTube anti-bot protection, export cookies from your browser:
# 1. Install "Get cookies.txt" Chrome extension  
# 2. Go to YouTube while logged in
# 3. Click the extension to export cookies.txt
# 4. Place the file in the server directory and set the path below
COOKIES_PATH=cookies.txt

# Example paths:
# COOKIES_PATH=/path/to/your/cookies.txt
# COOKIES_PATH=./server/cookies.txt